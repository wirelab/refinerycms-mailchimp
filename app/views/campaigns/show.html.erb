<% content_for :body_content_subject do %>
  <%= @campaign.subject %>
<% end %>

<% content_for :body_content_left do %>
  
  <div>
    <h3>Title</h3>
    <%=raw @campaign.subject %>
  </div>
  
  <div>
    <h3>Body</h3>
    <%=raw @campaign.body %>
  </div>
  
<% end %>

<% content_for :body_content_right do %>
  <h2><%= t('.other') %></h2>
  <ul id="campaigns">
    <% @campaigns.each do |campaign| %>
      <li>
        <%= link_to campaign.subject, campaign_url(campaign) %>
      </li>
    <% end %>
  </ul>
<% end %>

<%= render :partial => "/shared/content_page" %>
